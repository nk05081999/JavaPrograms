public class RatInMazeg13 {
    // Given an N*N Grid where some cells are blocked(denoted by 0)
    //  and other are not blocked denoted by 1
    // a rat starts at the top left cell and need to reach at bottom right cell.
    // Ther rat can move down right up left but cannot move in blocked cells.
    // we need to fin all the possible paths.


    // Approach:
    // start at (0,0),mark it as visted.
    // try moving in all four directions (D,U,L,R)
    // if the desitnation reacged,add the current path to res.
    // if moving leads to invalid,backtrack.
    // use a boolean array to keep track of vistsed cells.


    
    
}
