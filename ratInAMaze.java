public class ratInAMaze {
    // Given an N*N grid where some cells are blocked (denoted by 0)and other are 
    // Passable(denoted by 1), a rat starts at the left cell and needs to reach 
    // the botton right cell.
    // The rat can move down,right,up or left but cannot move throughh the blocked
    // cells

    // We need to find all the possible paths.


    // Approach:
    // strat at (0,0),mark it as visted.
    // try moving in all four directions(down right ,left,up).
    // if the destination raeched ,add the current path to the result.
    //  backtrack.
    // use boolean visted array to keep track of visted cells.
    
}
