public class SujalTowersOfHanoi {
    // You are given a three rods and n dics  stacked in one rod in an incresing
    // order of size frm top to button

    // ONLY ONE DISC  CAN BE moved at a time

    // A larger disc canot be placed on a smaller disc

    // you can use use three rods./....


    // Resurcive Formula=T(N)=2T(N-1)+1;

    // T(1)=1

    // Time com=O(2^n)
    // space complexity=  O(n)

    // Alogorith:

    // Move N-1 disks from source to  Auxiliry()
    // Move the Nth disk directly from source to Desitnation

    // Move N-1 disks from Auxiliry to Destination(source helper)
    
}
