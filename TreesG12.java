public class TreesG12 {
    // A tree is a hierachicel data structure consisting of nodes,with a 
    // root node at the top and child nodes below.

    // Trees are widely used in applications such as databases,compilers etc.


    // types of Trees:

    // Binary Tree: Each node has atmost two childrens(left and right)
    // Binary search tree(BST):A binary tree where:
    // left child<parent
    // right child >parent

    
    
}
